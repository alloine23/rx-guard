Place test prescription/medical record images here.

The test `test_real_images.py` will pick up any `.png`, `.jpg`, or `.jpeg`
file in this folder and run the full OCR pipeline against it, printing
extracted fields so you can verify correctness.

Images in this folder are gitignored to avoid bloating the repo.
